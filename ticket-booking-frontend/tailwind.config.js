/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: "#6366F1",   // Electric Indigo
        secondary: "#FF6B6B", // Urgency / errors
        dark: "#0F172A",      // Deep Charcoal
        light: "#F8FAFC",     // Off-white
      },
      animation: {
        'pulse-fast': 'pulse 1s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      }
    },
  },
  plugins: [],
}
